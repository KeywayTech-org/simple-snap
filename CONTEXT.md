# CONTEXT.md — 拾景纸刊 Demo 领域词汇表

> 本文件是本仓库的"业务词典"。只收录术语、概念边界、关系；不写实现、不写规格。

## 角色与对象

- **用户 (User)**
  打开 H5 页面、上传照片、看到结果的人。Demo 阶段不登录、不持久化。

- **源照片 (Source Photo)**
  用户上传的原始图像。链路中**只读不写**。可能含人脸、证件照等敏感内容。

- **创作提示词 (Generation Prompt)**
  按 scenes-gathered-zine-v1-3 规定的 4 段式 prompt 文本。不可被用户看到/编辑。

- **作品 (Poster)**
  GPT-image 根据创作提示词 + 源照片生成出的最终图。3:5 竖版。

- **创作思路 (Creative Rationale)**
  链路中给用户看的 1 段中文说明，解释"这张图做了什么"。默认折叠收起。

## 流程阶段

- **上传 (Upload)**
  把源照片从客户端传到后端。失败提示"上传失败，请重试"。

- **解读 (Interpret)**
  大模型读图、按 4 段式模板生成创作提示词。不暴露给用户。

- **创作 (Generate)**
  GPT-image 接收源照片 + 创作提示词，输出作品图。

## 选项

- **高纯度色 (Hue)**
  单一附加色，5 选 1：红 / 蓝 / 黄 / 绿 / 品红；额外"AI 决定"选项。色相是结构性的，不是装饰。

- **文字语言 (Text Language)**
  微文本语言模式：英文 / 中文 / 双语 / 无。`英文` 是默认；`中文` 与 `双语` 走特殊长度限制（中文 ≤ 8 汉字，英文 ≤ 5 词）。

- **微文本 (Micro-text)**
  嵌入作品图中的小字。≤ 5 英文词 / 8 汉字。如果用户没输入文字，由链路自动生成。

## 状态

- **空状态 (Empty)**
  未上传。页面只显示上传区。

- **处理中 (Processing)**
  处于"上传 / 解读 / 创作"任一阶段。显示当前阶段 label + 进度条。

- **结果 (Result)**
  作品已出。展示作品图 + 操作按钮 + 创作思路。

- **失败 (Failed)**
  任一阶段超时或被拒。Toast 提示后回到空状态或保留源照片。

## 不在 Demo 范围（明确划界）

- 用户账号 / 登录 / 鉴权
- 历史作品 / 我的作品列表
- 任何形式的持久化存储（数据库、对象存储、缓存）
- 付费 / 配额 / 分享
- 内容安全扫描（v1 demo 信任 LLM 与 GPT-image 自身的拒生）
- 营销页 / Hero / 介绍页

## 关键概念边界

- **创作提示词 ≠ 创作思路**：
  提示词是喂给 GPT-image 的输入（用户看不到），思路是给用户看的解释（中文 1-3 句）。
- **源照片 ≠ 作品**：
  源照片是事实，作品是源照片的"拾景纸刊"风格再创作。
- **重做 ≠ 第一次**：
  第一次无选项（全部走默认），重做可在抽屉里改"高纯度色 / 文字语言 / 微文本"。
